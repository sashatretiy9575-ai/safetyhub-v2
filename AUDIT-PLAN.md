# AUDIT-PLAN.md — живой чеклист устранения находок AUDIT.md

Источник: `AUDIT.md` (321 находка, 9 сентября 2026). План работ:
`.claude/plans/audit-md-bubbly-hamming.md`.

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` сделано · `[-]` пропущено · `[!]` нужно действие владельца.

## Сводка

| Пункт | Тема | Находок |
| --- | --- | --- |
| 2 | Capability-модель и ложные гарантии тестов | 5 |
| 3 | Секреты, гейты и заголовки безопасности | 15 |
| 4 | Валидация, квоты и границы API | 24 |
| 5 | SQL-миграции и Edge Functions | 14 |
| 6 | Критический путь: экран теста | 15 |
| 7 | Модалки админки и оболочка | 14 |
| 8 | Гонки и надёжность массовых операций | 15 |
| 9 | Редакторы контента | 29 |
| 10 | PWA: баннер, установка, идентичность | 15 |
| 11 | Шрифты | 7 |
| 12 | Переводы и словари | 31 |
| 13 | Маршрутизация локалей и SEO-инфраструктура | 19 |
| 14 | SEO: тексты, метаданные, JSON-LD | 29 |
| 15 | Семантика заголовков и доступность | 17 |
| 16 | Производительность: изображения, бандлы, service worker | 21 |
| 17 | Мёртвый код и зависимости | 29 |
| 18 | Порядок проверок в route handlers | 3 |
| — | Пропущено осознанно | 19 |

## Пункт 2. Capability-модель и ложные гарантии тестов

- [x] `B1-02` P0 — requireCapability и requireAnyCapability игнорируют аргумент: вся capability-модель приложения сведена к проверке роли admin
- [x] `B1-05` P1 — Тест baseline-admin-security даёт ложную гарантию: проверяет константу, которую не читает ни один рантайм-модуль
- [x] `B1-19` P2 — Шесть админских роутов работают service-role клиентом в обход RLS: для них no-op requireCapability — единственная авторизация
- [x] `B1-20` P2 — safeRedirectPath и authenticatedLandingPath — мёртвые экспорты; реальная валидация return-адреса живёт в клиенте и молча теряет большинство маршрутов
- [x] `B2-25` P2 — Мёртвый механизм точечных полномочий: CAPABILITY_LABELS, SUPERADMIN_ONLY_CAPABILITIES и setAdminCapabilities

## Пункт 3. Секреты, гейты и заголовки безопасности

- [x] `B1-07` P1 — 'unsafe-eval' в script-src обеих CSP-политик из-за клиентского pdf.js в админ-загрузчике
- [x] `B1-14` P2 — Гейт check:security не сканирует .md/.toml/.py/.ps1 и не знает паттернов строк подключения и Turnstile-секретов
- [x] `B1-15` P2 — Захардкоженный секрет подписи сертификатов в lib/certificates/verification.ts и отсутствие server-only маркера
- [x] `B1-16` P2 — Антиабузный HMAC-ключ молча подменяется service_role-ключом Supabase
- [x] `B1-17` P2 — Роут send-email пишет в лог адрес почтового ящика и точную длину его пароля
- [x] `B1-18` P2 — .gitignore не покрывает .env.development / .env.production / .env.test, которые Next.js загружает автоматически
- [x] `B1-32` P2 — Страница /verify/[token] с персональными данными отдаётся под слабой публичной CSP (script-src 'unsafe-inline')
- [x] `B1-33` P2 — Origin Supabase попадает в публичную статическую CSP, а тест, который это запрещает, зависит от окружения и всегда зелёный
- [x] `B1-34` P2 — Гейт no-store и правило /api/:path* не покрывают route handlers вне app/api, включая приватную выдачу презентаций
- [x] `B1-35` P2 — Permissions-Policy перечисляет шесть функций, из них одна удалённая, и оставляет browsing-topics с дефолтом *
- [x] `B1-36` P2 — Ни одна из двух CSP не имеет отчётности (report-to / Reporting-Endpoints), поэтому ужесточить политику нечем
- [x] `B1-41` P2 — Логин почтового ящика и длина SMTP-пароля пишутся в журнал Vercel
- [x] `B1-48` P2 — revalidate принимает публичный плейсхолдер CONTENT_REVALIDATE_SECRET из .env.example
- [x] `B1-50` P2 — Email получателя и имя SMTP-ящика попадают в логи Vercel в обход собственного запрета на логирование
- [x] `B1-53` P2 — parseHookSecrets не проверяет длину секрета вебхука и принимает пустой ключ HMAC

## Пункт 4. Валидация, квоты и границы API

- [x] `B1-08` P1 — Хук /api/auth/send-email читает тело целиком до проверки подписи; лимит 64 КБ обходится
- [x] `B1-09` P1 — Схема identity расходится с ограничениями БД и не фильтрует управляющие символы
- [x] `B1-10` P1 — Админский verify-identity принимает organization длиннее колонки БД и любые управляющие символы
- [x] `B1-11` P1 — Граница ADMIN_ATTESTATION_BULK_LIMIT на /attestations/selection отдаётся как 500 SERVER_ERROR вместо 4xx
- [x] `B1-22` P2 — Лишний service-role вызов Auth Admin API: результат getUserById выбрасывается, но задерживает ответ и расширяет привилегированную поверхность
- [x] `B1-23` P2 — Коарс-квота admin.purge списывается дважды за один запрос — бюджет вдвое меньше заявленного
- [x] `B1-29` P2 — Zod 4: .shape обходит объектный superRefine — проверка HTTPS у sources не работает ни на одном пути записи
- [x] `B1-30` P2 — coerceContentMetadata тихо стирает юрисдикцию, дату и все источники при одной плохой ссылке
- [x] `B1-31` P2 — Два разных валидатора для одного понятия «ссылка на источник»: строгий для блока и слабый для метаданных
- [x] `B1-38` P2 — Параметр startNew принимается схемой и маршрутом, но никогда не доходит до БД
- [x] `B1-39` P2 — isAsset проверяет параметр маршрута оператором in — проходят ключи прототипа Object
- [x] `B1-42` P2 — Источники и метаданные курса/статьи не валидируются ни на одном админском пути записи, а публичное чтение молча выбрасывает их целиком
- [x] `B1-43` P2 — PATCH /api/admin/settings/contacts — единственный админский роут без zod: ручные typeof-проверки без границ
- [x] `B1-44` P2 — Массовое изменение профиля: три несогласованных лимита длины (UI 200 / API 200 / БД 160 и 80)
- [x] `B1-45` P2 — Половина схем тела админских запросов не .strict(): лишние ключи принимаются молча
- [x] `B1-46` P2 — Границы массовых операций заданы литералами 100/500 вместо именованных констант — четыре независимые копии
- [x] `B1-49` P2 — Хук send-email буферизует тело целиком до проверки размера и подписи
- [x] `B1-54` P2 — Миниатюра презентации не имеет лимита байт при финализации, но отдаётся через ограничитель в 5 МБ — превью ломается навсегда
- [x] `B1-55` P2 — Дедупликация медиа-ассета по sha256 игнорирует статус строки и возвращает ссылку на уже удалённый файл
- [x] `B1-56` P2 — Загрузка медиа-ассета доверяет заголовку Content-Length вместо ограниченного чтения тела
- [x] `B1-58` P2 — Смена e-mail во входе не сбрасывает кулдаун из sessionStorage — отправка кода блокируется чужим таймером
- [x] `B1-59` P2 — draftHeaders удаляет ключи во время прямого обхода индексов: LRU-лимит и очистка битых черновиков работают через раз
- [x] `B1-60` P2 — Clear-Site-Data: "cache", "storage" выставляется на обычном протухании сессии в proxy — сносит регистрацию service worker и HTTP-кэш origin
- [x] `B2-04` P1 — Два клиентских мутирующих запроса идут мимо clientRequest и остаются без таймаута, вопреки собственному gate-тесту

## Пункт 5. SQL-миграции и Edge Functions

- [x] `B1-04` P0 — Одна невалидная строка презентации обрушивает весь запуск storage-reconciler и все prune-задачи
- [x] `B1-12` P1 — storage-reconciler не знает про locale-сегмент в пути публичной презентации — весь прогон падает с 500
- [ ] `B1-21` P2 — /api/content-assets/[assetId] отдаёт любой активный медиа-ассет без аутентификации через service-role ключ
- [ ] `B1-24` P2 — Квота актора возвращается назад при ошибке в трёх метрируемых RPC — нарушен явно задокументированный инвариант
- [ ] `B1-25` P2 — Сетевой бюджет отправки кодов вдвое больше почасового лимита писем всего проекта — одна подсеть выключает вход для всех
- [x] `B1-26` P2 — HEAD-запрос за презентацией курса проходит мимо обеих квот скачивания
- [x] `B1-27` P2 — Скачивание готового задания на экспорт сертификатов не ограничено частотой и переигрывается 30 минут подряд
- [x] `B1-28` P2 — Два админских POST-эндпоинта выполняют тяжёлые выборки вообще без квоты
- [x] `B1-37` P2 — Инъекция LIKE-шаблона в /api/profile/organizations: символы % и _ не экранируются
- [ ] `B1-47` P2 — Аудит, синхронизация и инвариант ролей привязаны к устаревшей колонке role, тогда как авторизация уже читает product_role
- [ ] `B1-52` P2 — Standard Webhooks в send-email: нет защиты от повтора внутри пятиминутного окна
- [ ] `B1-57` P2 — Бакет content-media публичный — байты доступны напрямую и после снятия ассета с публикации
- [ ] `B3-37` P2 — Блог и sitemap жёстко обрезаны на 50 статьях: SQL-функция не даёт больше, а пагинации нет
- [x] `B3-38` P2 — sitemap.xml игнорирует флаг seo.indexable — в карту попадают URL, помеченные noindex — TS-половина: sitemap фильтрует по `seo.indexable`. **SQL-половина не сделана** — чтобы флаг работал и для курсов в локализованном режиме, нужна новая forward-only миграция `list_published_courses_locale` с полем `seo`; вынесено в отложенный SQL пункта 5

## Пункт 6. Критический путь: экран теста

- [x] `B3-05` P1 — Экран теста: h1 — это текст вопроса, название курса нигде не заголовок, а состояния загрузки/ошибки вовсе без h1
- [x] `B3-33` P2 — Экран результата теста: переходы на /profile и /onboarding — кнопки с router.push вместо ссылок
- [x] `B4-11` P1 — Тест: предупреждение о несохранённых ответах не срабатывает — эффект читает ref, которого нет в зависимостях
- [ ] `B4-15` P1 — Мёртвый булев флаг startNew протянут через 4 слоя; имя _startNew противоречит вызовам
- [x] `B4-16` P1 — Число попыток в день зашито как 8 в тексте ошибки, хотя оно настраивается администратором на курс
- [x] `B4-43` P2 — Порог «срочности» таймера, шаг тика и смещение буквы варианта заданы литералами в quiz-client
- [x] `B4-44` P2 — useEffect предупреждения о несохранённых ответах читает ref, а перезапускается по неиспользуемому в теле saveState
- [x] `B4-47` P2 — Однобуквенные имена q и a в quiz-client при полном именовании во всём остальном файле
- [x] `B5-01` P0 — Радиокнопки теста скрыты через sr-only — у клавиатурного фокуса нет видимого индикатора
- [x] `B5-02` P0 — Захардкоженный text-white поверх bg-[var(--color-primary)] даёт 2.33:1 в тёмной теме
- [x] `B5-04` P0 — Навигация по 10 вопросам теста не переносится и обрезается: с 5-го вопроса кнопки недоступны
- [x] `B5-26` P2 — Иконки на --color-accent-amber-soft в светлой теме дают 1.93:1 (порог 3:1)
- [x] `B5-35` P2 — Уход с активной попытки теста через шапку, док или футер не предупреждается
- [x] `B6-23` P1 — RU: одна и та же ссылка на страницу курса подписана «К теме» и «К курсу»
- [x] `B6-29` P2 — Home.process.learnText жёстко зашивает форму «вопросов» вместо ICU-плюрала в ru

## Пункт 7. Модалки админки и оболочка

- [x] `B4-41` P2 — z-index задан произвольными литералами в восьми слоях без единой шкалы токенов
- [x] `B5-03` P0 — container-type: inline-size в админке ловит все position:fixed — модалки уезжают в середину документа
- [x] `B5-05` P0 — Разрыв 808–1023 px в админке: включается «десктопная» вёрстка, но остаётся мобильный нижний док
- [x] `B5-10` P1 — Панель действий редактора прилипает под шапкой админки в диапазоне 768–1023 px
- [x] `B5-11` P1 — Панель уведомлений админа с z-[80] заперта в контексте наложения шапки z-40 и уходит под нижний док
- [ ] `B5-18` P1 — Оба скип-линка приземляют пользователя под липкую шапку, а в админке цель ещё и не фокусируема
- [ ] `B5-19` P1 — Панель фильтров аттестаций блокирует прокрутку и запирает фокус на десктопе, где она не модальна
- [x] `B5-25` P2 — Панель уведомлений админа объявлена role="dialog", но фокус в неё не переносится
- [x] `B5-27` P2 — Колонка действий таблицы аттестаций задана 32 px, а кнопка внутри — 44 px и наезжает на статус
- [ ] `B5-28` P2 — Редактор материалов в админке переходит на три колонки по вьюпорту (lg:), игнорируя сужение рабочей области сайдбаром
- [x] `B5-29` P2 — Мёртвое правило: резерв под мобильный док сбрасывается на 1440 px, хотя док исчезает на 1024 px
- [x] `B5-31` P2 — Три из пяти модальных окон админки не закрываются кликом по подложке
- [x] `B5-32` P2 — DestructiveDialog не блокирует прокрутку фона, в отличие от остальных модалок проекта
- [x] `B5-33` P2 — Панель уведомлений админки не возвращает фокус на колокольчик после закрытия

## Пункт 8. Гонки и надёжность массовых операций

- [x] `B4-01` P0 — Гонка при выборе компании в аттестациях: удалить/переименовать можно чужих сотрудников
- [x] `B4-02` P1 — Инбокс: одна нераспознанная запись обнуляет всю страницу уведомлений
- [x] `B4-06` P1 — Массовое удаление уходит в вечный IDEMPOTENCY_KEY_REUSED: ключи не сбрасываются после ошибки
- [x] `B4-07` P1 — «Изменить название компании» из меню полосы открывает диалог до разрешения выборки: «0 чел.» и 400 INVALID_REQUEST
- [x] `B4-14` P1 — Очередь заявок: setTimeout на router.refresh не очищается, состояние продублировано в трёх местах
- [ ] `B4-23` P2 — Инбокс: «Показать предыдущие» отменяется первым же успешным опросом или действием администратора
- [x] `B4-28` P2 — Частичный отказ массового удаления теряет уже удалённых: список не обновляется, оператору сказано «не выполнено»
- [x] `B4-29` P2 — Web Worker генерации сертификатов не останавливается при размонтировании панели аттестаций
- [ ] `B4-33` P2 — Снятие одной галочки молча сбрасывает серверную выборку «все строки по фильтру» до одной страницы
- [ ] `B4-35` P2 — Производное состояние синхронизируется через useEffect вместо вычисления при рендере — полный список
- [x] `B4-36` P2 — Медиатека CMS: запрос без отмены и без учёта в зависимостях, setState после размонтирования
- [x] `B7-03` P1 — Экспорт сертификатов не отменяется при размонтировании: Worker и скачивание ZIP продолжаются
- [x] `B7-07` P1 — Список аттестаций: строки не мемоизированы, ключ компании нормализуется O(строк × компаний) раз за рендер
- [x] `B7-23` P2 — Инбокс уведомлений: новый Intl.DateTimeFormat на каждое уведомление при каждом рендере
- [x] `B7-26` P2 — Поиск похожих компаний: нормализация строки выполняется внутри внутреннего цикла, O(n²) лишних операций

## Пункт 9. Редакторы контента

- [ ] `B2-50` P2 — previewFont, statusRecord и тип SaveResponse скопированы в трёх редакторах локализаций; китайский стек шрифтов задан трижды
- [x] `B4-03` P1 — Редактор юридических документов: сохранение локализации перебрасывает оператора на вкладку RU
- [x] `B4-04` P1 — Редактор статей: локальный черновик восстанавливается молча, а для новых статей ключ хранилища один на всех — 'new'
- [x] `B4-05` P1 — Редактор курса молча теряет правки, сделанные во время сохранения (setCourse из устаревшего замыкания)
- [x] `B4-08` P1 — Юридический редактор сбрасывает вкладку и текст локализации на каждом сохранении
- [x] `B4-12` P1 — Кнопка скачивания сертификата: неочищаемый setTimeout сбрасывает состояние поверх новой загрузки
- [x] `B4-13` P1 — Автосохранение статьи может отправить два параллельных saveArticleAction с одной и той же draftVersion
- [x] `B4-17` P1 — validateTestEditor игнорирует собственный TEST_EDITOR_LIMITS в пяти проверках, а все тексты ошибок дублируют числа литералами
- [x] `B4-18` P1 — Магическое 30 (3 варианта x 10 вопросов) зашито в четырёх местах редактора теста
- [ ] `B4-19` P1 — apiError: 134-строчная лестница подстрочного поиска по тексту ошибки вместо таблицы кодов
- [ ] `B4-20` P2 — Два источника истины для контракта уведомлений: 190 строк ручной валидации дублируют zod-схему
- [x] `B4-22` P2 — Редактор статей: две полные zod-валидации всей статьи и JSON.stringify документа на каждое нажатие клавиши
- [ ] `B4-24` P2 — Панели аттестаций: мёртвая ветка individual-update и недостижимое поле bulk-update field:'job'
- [x] `B4-25` P2 — Редактор статей и OTP-форма: состояния, которые только записываются и никогда не читаются
- [x] `B4-27` P2 — После сохранения черновика опубликованный курс показывается как «Снят с публикации»
- [ ] `B4-34` P2 — Глобальный изменяемый кеш доступа к материалам курса в модульной переменной
- [ ] `B4-38` P2 — Вложенные тернарные цепочки глубиной 3-4 в JSX и в выборе HTTP-статуса — пять мест
- [x] `B4-39` P2 — AvatarUploader хардкодит 360x360 вместо AVATAR_WIDTH/AVATAR_HEIGHT из модуля, который сам же импортирует
- [x] `B4-40` P2 — Дублированная функция dateBoundary с булевым флагом и разными именами параметра в двух модулях
- [x] `B4-42` P2 — Сломанная плюрализация: «исправьте 2 ошибок» — условие на единственное число вместо трёх форм
- [ ] `B4-45` P2 — Позиционные булевы флаги в вызовах save(true)/save(false), refreshQueue(true) и validateTestEditor({ publish })
- [ ] `B4-46` P2 — Число вариантов ответа и максимум длительности зашиты литералами в схеме разбора попытки
- [x] `B4-48` P2 — Таймауты и задержки скачивания сертификата заданы литералами на месте вызова
- [ ] `B5-15` P1 — Локализационные редакторы (курсы, статьи, юр. документы) не защищены от ухода со страницы
- [x] `B5-16` P1 — После сохранения перевода редактор юридических документов молча возвращает вкладку на «Русский»
- [ ] `B5-34` P2 — Кнопка «Назад» в редакторе статей мертва при прямом открытии адреса редактора
- [x] `B7-04` P1 — Редактор теста: полная валидация курса выполняется дважды на каждое нажатие клавиши
- [x] `B7-06` P1 — Редактор статьи: полный разбор Zod-схемы блоков выполняется в теле компонента на каждый рендер
- [x] `B7-25` P2 — Редактор статьи: предпросмотр валидируется по Zod даже когда предпросмотр закрыт

## Пункт 10. PWA: баннер, установка, идентичность

- [x] `B2-01` P1 — Кнопка установки PWA ведёт на несуществующий маршрут /install (404)
- [x] `B2-05` P2 — Задержка 15 с и требование взаимодействия в PWA-баннере мертвы: состояния инициализированы значением true
- [x] `B2-08` P2 — Тест доступности PWA проходит по комментарию: медиазапрос не содержит pointer: coarse
- [x] `B2-13` P2 — Избыточное условие в вычислении видимости PWA-баннера: (isInstallable || !isStandalone) поглощается соседним !isStandalone
- [x] `B2-46` P2 — Блок PWA-идентичности (manifest/icons/appleWebApp) скопирован в четыре корневых layout, значения уже разошлись
- [x] `B4-09` P1 — Три независимых экземпляра usePwaInstall и мёртвый PWAProvider без единого потребителя
- [x] `B4-10` P1 — Баннер установки PWA: задержка 15 с и ожидание взаимодействия — мёртвый код, состояние инициализировано в true
- [x] `B5-12` P1 — В приватных корнях (кабинет и админка) не объявлен viewportFit: 'cover' — вся работа с safe-area мертва
- [x] `B5-13` P1 — Баннер установки PWA показывается мгновенно и резервирует 160 px вместо реальных 204 px + safe-area
- [x] `B5-17` P1 — Пункт меню «Установить приложение» ведёт администратора на несуществующий якорь
- [x] `B5-20` P1 — Задержка показа PWA-баннера и ожидание взаимодействия отключены: оба флага инициализированы значением true
- [x] `B5-21` P1 — Резерв места под PWA-баннер повешен на <main>, поэтому баннер перекрывает низ футера
- [x] `B5-30` P2 — Определение «телефона» для баннера установки — 899 px, а мобильный док живёт до 1023 px: рассинхрон в 900–1023 px
- [x] `B7-05` P1 — PWA-баннер: таймер задержки 15 с и три слушателя ввода стали мёртвым кодом
- [x] `B7-16` P1 — Баннер установки PWA: задержка 15 с и ожидание взаимодействия мертвы — гарантированный CLS на мобильных

## Пункт 11. Шрифты

- [x] `B7-02` P0 — Шрифт Manrope не содержит казахских букв ә ғ қ ң ұ — весь kk-локаль рендерится вперемешку с системным шрифтом — **сделано иначе, чем в аудите:** в самом Manrope (Google Fonts, `Manrope[wght].ttf`) этих десяти глифов нет вообще, пересборка сабсетов их дать не может. Десять кодовых точек взяты из Inter (`scripts/subset-kazakh-companion-font.py`, 4 статических начертания по 2.4 КБ), подключены как семейство `SafetyHub Kazakh` с `size-adjust: 98.9%` и подбором веса по толщине штриха; из `unicode-range` Manrope эти точки убраны. Регресс — `tests/i18n/font-coverage.test.mjs`
- [x] `B7-09` P1 — CJK-шрифт 16.4 МБ (несжатый OTF) скачивается в браузер при выдаче zh-сертификата — 16 437 364 → 3 553 936 байт (GB/T 2312 + корпус zh + латиница/кириллица), `scripts/subset-cjk-certificate-font.py`, версия `v=Sans2.005` синхронно в 5 местах, `MAX_FONT_BYTES` 24 → 6 МБ
- [x] `B7-10` P1 — Основной шрифт ru/kk (manrope-cyrillic.woff2) не предзагружается — preload есть только у китайского — preload латиницы для всех локалей и кириллицы для ru/kk; ветка zh сохранена
- [x] `B7-13` P1 — Нет favicon.ico: proxy считает путь существующим, а файла в проекте нет — 404 на каждый запрос иконки — `app/favicon.ico` 16/32/48 из мастера 512 px (`scripts/build-favicon.mjs`). Явные записи в `metadata.icons` не добавлял: Next уже отдаёт `<link rel="icon" href="/favicon.ico" sizes="48x48">` из файловой конвенции рядом с PNG-иконками (проверено в собранном HTML)
- [x] `B7-14` P1 — Иероглифы 中文 в OG-картинке не отрисовываются: у встроенного шрифта next/og нет CJK-глифов — `RU · KK · EN · ZH`
- [x] `B7-22` P1 — Шрифты Manrope: нет preload и нет иммутабельного Cache-Control — FOUT и ревалидация 4 файлов на каждой навигации — контент-адресуемые имена всех 5 шрифтов + `Cache-Control: immutable` по маске в `next.config.ts`; preload (см. B7-10). **Шаг 3 (fallback с `size-adjust` под системный шрифт) пропущен:** одно значение не подходит одновременно Segoe UI (x-height 0.500) и Arial (0.519), а порядок применения `size-adjust` и `ascent-override` различается между движками; preload закрывает то же окно FOUT без этого риска
- [x] `B7-30` P2 — Китайский сабсет собран только по messages/zh.json — 865 из 1015 иероглифов контента им не покрыты — сабсет пересобран по `messages/zh.json` + всем `content/localizations/**/zh.json`: 187 → 1164 кодовых точек, 47 364 → 335 900 байт (лимит теста 500 000). `unicode-range` для этого @font-face **не добавлял**: семейство упоминается только в zh-стеке, который подменяет `--font-sans` целиком, и обязано рисовать в том числе латиницу и цифры

## Пункт 12. Переводы и словари

- [x] `B3-09` P2 — Два мёртвых неймспейса рекламной воды: Home.extraBenefits и Home.format — 26 ключей × 4 словаря, ни один не рендерится — `Home.extraBenefits` и `Home.format` (22 ключа × 4) удалены. `BRAND.tagline` оставлен для `B3-52`
- [x] `B3-22` P2 — h1 страниц 404 и ошибки — на английском внутри русского и локализованных разделов — `app/error.tsx` и `app/not-found.tsx` переведены по схеме `global-error` (без `useTranslations` — запрещён `app-error-states.test.mjs`), детектор локали вынесен в `i18n/emergency-locale.ts`. Добавлены `app/(public)/not-found.tsx` и `app/[locale]/(public)/not-found.tsx` — теперь `notFound()` из публичного дерева отдаёт 404 в оболочке и на языке страницы
- [x] `B6-01` P0 — Заглушка переключателя темы 44 px против ~104–115 px реальной кнопки — прыжок шапки после гидратации — десктопная кнопка темы получила фиксированную ширину `7.25rem`, заглушка повторяет её только под `min-[1024px]:`
- [x] `B6-02` P0 — ZH-локаль: страница «пароли отменены» уверяет, что паролей нет, хотя китайский реалм — логин+пароль — правлен только `messages/zh.json` (ru/kk/en пинятся e2e). Редирект zh→login из «дополнительно» не делал: это смена поведения, а текст теперь и так соответствует реальному реалму
- [x] `B6-03` P1 — Весь китайский экран входа/регистрации захардкожен мимо next-intl — новый неймспейс `AuthZh` (29 ключей × 4 словаря), компонент переведён на `useTranslations`; имена `CAPTCHA_RETRY`/`AUTO_LOGIN_FALLBACK` сохранены (пинятся `turnstile-lifecycle.test.mjs`), значение `创建访问账号` сохранено посимвольно ради `e2e/zh-admission.spec.ts`
- [x] `B6-04` P1 — Контактный блок главной и FAQ отдаёт город и часы работы только по-русски во всех локалях — `Shell.footer.city/hours` вместо `CONTACT_DETAILS`; осиротевшая константа удалена из `lib/constants.ts`
- [x] `B6-05` P1 — Экран восстановления доступа для zh: весь текст и ссылка захардкожены — три ключа `AuthOtp.zhRecovery*`, компонент стал async-серверным с `getTranslations` и `localizePathname`
- [x] `B6-06` P1 — Заглушка переключателя языка на 23 px уже реальной и теряет shrink-0 — второй прыжок шапки — заглушка переключателя языка получила `shrink-0` и распорку 15 px вместо каретки (иконка в LCP-бандл не тянется)
- [x] `B6-08` P1 — Английский Blog.readTime не склоняется: «12 minute read» вместо «12 minutes read» — вместе с `B6-19`
- [x] `B6-09` P1 — Карточки «как проходит обучение»: жёсткая высота h-[12rem]/h-[12.5rem] физически не вмещает объявленный line-clamp-3 — `h-[12.5rem]`/`min-[1200px]:h-[12rem]` → `h-full min-h-[…]`: равные высоты держит grid (это и проверяет e2e), верхнюю границу теперь задаёт контент, и третья строка `line-clamp-3` помещается
- [x] `B6-10` P1 — Колонка статусов в таблице курсов кабинета зафиксирована на 11rem без запаса и без переноса — трек статусов 11rem → 13rem в обеих точках (строка и шапка), контейнеру бейджей разрешён перенос
- [x] `B6-11` P1 — Мёртвый ключ Profile.passwordlessHint, в китайской версии описывающий несуществующий вход по passkey — закрыт удалением ключа (см. `B6-22`); вариант B из аудита не нужен
- [x] `B6-17` P1 — Мёртвый код: определение локали по Accept-Language и cookie (resolvePreferredLocale/localeFromAcceptLanguage) не используется нигде в приложении — `LANGUAGE_ALIASES`, `localeFromAcceptLanguage`, `resolvePreferredLocale` удалены; тест заменён на проверку отсутствия (иначе единственным потребителем мёртвого кода оставался тест)
- [x] `B6-18` P1 — Два целых блока словаря (Home.extraBenefits и Home.format, 22 ключа × 4 локали) не рендерятся нигде — вместе с `B3-09`
- [x] `B6-19` P1 — EN: сломанная ICU-множественность времени чтения — всегда «7 minute read» — `en Blog.readTime` получил вторую форму; заодно `en Course.pagesShort` стал ICU-плюралом
- [x] `B6-20` P1 — EN/ZH: «охрана труда» переведена тремя разными терминами, канонический термин из глоссария не использован ни разу — канон глоссария применён в `Topics.*` и `Metadata.description` для en/zh. **`Metadata.manifestName` не трогал:** смена меняет имя уже установленного PWA на устройствах — нужно решение владельца
- [x] `B6-21` P1 — RU: в ответе FAQ пропущено существительное — «ответить минимум на 7» уезжает в JSON-LD FAQPage — существительное восстановлено, набор плейсхолдеров прежний
- [x] `B6-22` P1 — 17 ключей-сирот сверх мёртвых секций; среди них единственный текст про сброс лимита попыток — `Quiz.result.failedDescription` выведен на экран провала и дополнен третьим предложением в kk/en/zh; 16 остальных сирот удалены из всех четырёх словарей (каждый проверен grep-ом). `AppState.errorTitle/errorDescription` не удалены, а перенесены в `messages/global-error/*` — там у них появился потребитель (`app/error.tsx`)
- [x] `B6-24` P1 — Захардкоженные количества «5 курсов» и «10 материалов» в шапках, которые рендерятся даже при пустом каталоге — вариант А: непроверяемые числа убраны из `Topics.eyebrow` и `Blog.eyebrow` во всех четырёх словарях
- [x] `B6-25` P1 — ZH: весь UI китайского входа захардкожен мимо словаря, термины «账户»/«账号» разъезжаются — вместе с `B6-03`; термин приведён к `账号`, `AuthOtp.loginTitle` согласован с H1
- [x] `B6-26` P2 — Письма recovery и invite всегда уходят по-русски, игнорируя локаль получателя — таблица `NOTICE_COPY` на ru/kk/en, `page(payload.locale, …)` вместо `page('ru', …)`
- [x] `B6-27` P2 — Единицы времени для таймера повторной отправки кода живут отдельным словарём в коде — `AuthOtp.retryUnits` в четырёх словарях, `formatRetryDelay(seconds, units)`; заодно исправлена казахская «секунда» (была русская «с»)
- [x] `B6-28` P2 — Гео-мета и адрес в JSON-LD отдают «Алматы» кириллицей на всех локалях — `LOCALIZED_CITY` в `lib/seo.ts` для `geo.placename` и `addressLocality`. `BRAND.tagline` **оставлен** — он нужен для OG-картинки в `B3-52`
- [x] `B6-30` P2 — Подписи нижней таб-панели обрезаются truncate на узких экранах, которые e2e явно прогоняет — ниже 360 px кегль подписи уменьшается, `truncate` остаётся страховкой
- [ ] `B6-07` P1 — Вся e2e-проверка адаптива гоняется только по русскому реалму — регресс от длины перевода поймать нечем
- [x] `B6-32` P2 — Глобальная страница 404 без атрибута lang и с захардкоженным английским текстом на всех локалях — корневой 404 стал клиентским, читает `messages/global-error/*` и выставляет `lang` на `document.documentElement`. Собственный `<html>` **не рендерю**: корневого `app/layout.tsx` в проекте нет, документ синтезирует Next, и вложенный `<html>` дал бы невалидную разметку
- [x] `B6-36` P2 — ZH: стерильная подсказка входа описывает вход по passkey и PIN — способ аутентификации, которого в проекте нет — закрыт удалением ключа (см. `B6-22`)
- [x] `B6-37` P2 — Ни одного неразрывного пробела на 4 словаря: числа отрываются от единиц на узких экранах — U+00A0 в 8 ключах × ru/kk/en по списку аудита; эталон в `timers-and-contact-actions.test.mjs` обновлён
- [x] `B6-38` P2 — ZH: в хлебных крошках блог назван «博客», а в навигации по тому же URL — «文章» — `博客` → `文章` в крошках и кнопке «назад», две одинаковые кнопки разведены
- [x] `B6-39` P2 — EN: единственный прямой апостроф среди типографских — «We'll» вместо «We’ll» — U+2019
- [x] `B6-40` P2 — KK: «Обсудим вашу задачу» переведено как «обсудим вашу обязанность» — машинный выбор значения — «Міндетіңізді» → «Сұранысыңызды»

## Пункт 13. Маршрутизация локалей и SEO-инфраструктура

- [x] `B3-07` P1 — matcher прокси исключает любой путь с точкой — версионные юридические URL обходят fail-closed-защиту флага локалей — две правки: `isLocaleRoutablePath` больше не считает `/privacy/1.4` файлом (расширение обязано начинаться с буквы) и matcher прокси исключает статику по списку расширений вместо «любой путь с точкой». Проверено на собранном проде: `/kk/privacy/1.4` → 404
- [x] `B3-24` P2 — Идентичная FAQPage-разметка отдаётся и на главной, и на /faq (после включения локалей — на 8 URL) — FAQPage остаётся только на `/faq`; с главной снята разметка и вместе с ней `getFaqData`/`JsonLd`/`faqJsonLd`. Тест переписан на `assert.doesNotMatch`
- [x] `B3-35` P2 — sitemap.xml и hreflang размножают русский список слагов на все 4 локали — в карту попадают 404-URL — `availableLocales` добавлен в `SeoOptions` и в деструктуризацию `buildMetadata`; форма `localizedEntries` сохранена дословно (4 регэкспа), фильтрация вынесена в вызовы
- [x] `B3-36` P2 — /privacy и /privacy/{текущая версия} — байт-в-байт дубли с self-canonical; устаревшие версии полностью индексируемы — действующая версия каноникализируется на `/privacy` (`/terms`), устаревшие получают `noindex`. Блок про `robots.txt` из рецепта **не применял** — запрет обхода не даёт краулеру прочитать noindex. Заодно `noindex` вне превью стал `follow: true`, чтобы с устаревшей редакции был путь на действующую
- [x] `B3-39` P2 — Схлопывание /ru/* и редирект legacy-ссылок ?version= отдают 307 вместо постоянного 308 — 308 только при явно заданной версии: `?version=` с пустым значением резолвится в текущую, и постоянный редирект приколол бы читателя к устаревшей редакции
- [x] `B3-40` P2 — lastmod статических и юридических страниц захардкожен на 2026-08-19 и отстаёт от реального контента — `lastmod` для `/privacy`, `/terms`, `/blog`, `/topics` и главной выводится из реальных дат; проверено на проде: было 2026-08-19 у всех, стало 2026-09-05 / 2026-09-03
- [x] `B3-41` P2 — robots.txt закрывает /kk/onboarding, /en/onboarding, /zh/onboarding, но не корневой /onboarding — вместе с `B3-53` и `B6-33`: единый список `PRIVATE_PATHS`, из него строятся обе ветки. `curl /robots.txt | grep -c onboarding` → 4
- [x] `B3-42` P2 — Страницы без собственных alternates наследуют canonical главной: 404 курсов/статей и /verify указывают на «/» — обе ветки `return {}` отдают `alternates: { canonical: null }`; `app/not-found.tsx` получил `export const metadata` (конвенция резолвится последней и перекрывает лейауты). Для этого корневой 404 разделён: серверный файл с метаданными + клиентский `NotFoundNotice`
- [ ] `B3-45` P2 — Архивные версии политики и условий индексируются и полностью дублируют /privacy и /terms
- [x] `B3-53` P2 — robots.txt закрывает /onboarding только под языковыми префиксами, безпрефиксный путь пропущен — вместе с `B3-41`
- [x] `B6-12` P1 — dynamicParams=false + RU-слаги в generateStaticParams: локализованные /kk /en /zh страницы курсов и статей навсегда 404 — `getTopicSlugs(locale)` / `getArticleSlugs(locale)`; префиксные листы получили собственный `generateStaticParams` и `dynamicParams = true` вместо ре-экспорта русских слагов
- [x] `B6-13` P1 — sitemap.xml публикует /kk /en /zh URL для каждого русского курса и статьи без проверки существования локализации — sitemap собирает списки по каждой локали и выдаёт URL только там, где локализация есть
- [x] `B6-14` P1 — hreflang выдаётся для всех четырёх локалей безусловно — юридические версии 1.1/1.2/1.3 и 2.1/2.2/2.3 ссылаются на гарантированные 404 — `availableLocales` прокинут в юридические версии (по наличию файла локализации) и в страницы курсов/статей (новые `getTopicLocales`/`getArticleLocales` поверх уже кешированных списков)
- [x] `B6-15` P1 — Порядок проверок в proxy.ts: при выключенном флаге локалей /ru/* отдаёт 404 вместо схлопывающего редиректа — схлопывание `/ru/*` поднято выше fail-closed ветки. Проверено: с выключенным флагом `/ru/topics` → 308 на `/topics` (было 404), `/kk/topics` → по-прежнему 404
- [x] `B6-16` P1 — Схлопывание дубля /ru/* выполняется временным редиректом 307 вместо постоянного 308 — 308 вместо 307
- [x] `B6-31` P2 — Страницы теста и проверки сертификата наследуют canonical и hreflang главной страницы — экран теста получил собственный `path`, `/verify/[token]` — `alternates: { canonical: null }` (self-canonical опубликовал бы токен в разметке)
- [x] `B6-33` P2 — robots.txt: /onboarding закрыт только для префиксных локалей и открыт на русском маршруте — вместе с `B3-41`
- [x] `B6-34` P2 — JSON-LD объявляет четыре языка сайта, даже когда локальные маршруты отключены и обслуживается только русский — `servedLocales()` в `websiteJsonLd` и в `contactPoint.availableLanguage`
- [x] `B6-35` P2 — Permissions-Policy: catch-all в next.config.ts не учитывает локальные префиксы и накрывает /kk|/en|/zh/profile запретом camera=() — catch-all опущен ниже разрешающих правил и его lookahead расширен на локальные префиксы. Проверено: `/kk/profile` → `camera=(self)`, `/kk/topics` → `camera=()`

## Пункт 14. SEO: тексты, метаданные, JSON-LD

- [x] `B3-01` P1 — H1 главной не содержит ни одного коммерческого запроса: «Обучение по безопасности без лишней сложности» — H1 и подзаголовок главной переписаны; числа приходят из `QUIZ_POLICY`, а не набраны в четырёх словарях. Из формулировок аудита **убрал** два ложных утверждения, на которые указали его же ревьюеры: «сертификат в тот же день» (выдача ручная) и ссылку на статью ТК РК с Правилами проверки знаний (в репозитории не подтверждена, создаёт впечатление аккредитации)
- [x] `B3-02` P1 — Title и description главной — самые общие из возможных, description дословно дублирует Metadata.description — title 59 символов вместе с суффиксом (проверено на проде), description раскрыт и больше не дублирует `Metadata.description`
- [x] `B3-03` P1 — H1 всех пяти страниц курсов — одно слово («Плотник», «БИОТ», «Арматурщик») — новый ключ `Course.pageHeading`; нейтральный шаблон без подстановки предметной области — общий шаблон аудита давал «БИОТ: …по охране труда» (тавтология) и относил пожарную безопасность к охране труда
- [x] `B3-08` P1 — Title курсов — сырое название («БИОТ — SafetyHub.kz», «fire safety — SafetyHub.kz») — шаблон подставляется **только** когда `seo.title` совпал с названием курса (то есть админ поле не заполнял); `description` не трогаю — он выводится из реального описания курса и содержательнее шаблона
- [!] `B3-10` P2 — Слаги статей нацелены на поисковые запросы, а заголовки и title этих запросов не содержат — **действие владельца:** заголовки и слаги статей правятся повторной публикацией локализаций, а не кодом. Плюс ревьюер аудита показал, что предложенный заголовок для `pozharno-tehnicheskij-minimum` обещает программу ПТМ, которой в статье нет — сначала дописать раздел или сменить слаг
- [x] `B3-11` P2 — Каталог курсов: H1 «Курсы по безопасности» слабее собственного title, а в eyebrow вручную зашито «5 курсов» — из title/description убраны перечисление программ и числа политики: и состав каталога, и политика приходят из БД, поэтому такой текст протухает молча
- [!] `B3-12` P2 — Страница контактов: H1 равен title, а описание «Подскажем программу и ответим на вопросы» дублируется на трёх страницах — **действие владельца:** нужен подтверждённый физический адрес (улица и дом). Дубль описания снят вместе с `B3-47`
- [x] `B3-13` P2 — Блок «Возможности платформы» описывает интерфейс, а не услугу: «Учитесь с телефона, сохраняйте результат» — блок «Возможности платформы» переписан на условия обучения, числа через `QUIZ_POLICY` (`{count}`, `{pass}`, `{minutes}`, `{attempts}`)
- [x] `B3-14` P2 — Секции «Курсы» и «Как проходит обучение» на главной: заголовки-пустышки и шаги без единого факта — шаги приведены к реальному процессу (заявка → презентация и тест → результат и сертификат от администратора), числа из `QUIZ_POLICY`. Диапазон «25–59 страниц» из рецепта **не вносил** — он живёт в данных курса и устареет при перезаливке
- [!] `B3-15` P2 — Блог: H1-слоган вместо запроса, вручную зашитый счётчик «10 практических материалов» и два мёртвых ключа — счётчик «10 материалов» убран ещё в пункте 12 (`B6-24`). `Blog.cardEyebrow` («Практическое руководство» на всех карточках) оставлен: аудит назвал его проблемой, но замены не предложил, а осмысленный вариант зависит от рубрикации, которой в модели статьи нет
- [x] `B3-16` P2 — Блок статей на главной: «Безопасность на понятных примерах» и самопохвала «Без канцелярита и лишней теории» — заголовок сокращён до 52 символов и назвал темы; самопохвала «без канцелярита» заменена ссылкой на источник; пустое состояние больше не обещает «новые материалы готовятся»
- [x] `B3-17` P2 — h1 главной и /topics не содержат главный поисковый маркер «охрана труда», под который оптимизированы title/description/keywords — «охрана труда» теперь есть и в H1 главной, и в H1 каталога
- [x] `B3-23` P2 — PostalAddress собирается из строки «Казахстан, г. Алматы»: addressLocality и addressRegion получают одно и то же значение с названием страны внутри — `addressLocality` берётся из `LOCALIZED_CITY`, а не из видимой строки футера «Казахстан, г. Алматы»; `addressRegion`, дублировавший locality, снят
- [x] `B3-25` P2 — Article.author всегда Person с именем редакции: ни у одной статьи нет поля author — автор статьи стал `Organization` со ссылкой на `@id` организации: поля `author` нет ни в одной статье и ни в одном select-е, так что `Person` был выдумкой
- [x] `B3-26` P2 — LocalBusiness с точными координатами и часами приёма объявлен для компании без физического адреса на сайте — `localBusinessJsonLd` удалён вместе с координатами и часами приёма. Возврат — после подтверждения физического адреса (в действиях владельца)
- [x] `B3-27` P2 — Один и тот же курс получает разное имя провайдера на главной («SafetyHub») и на странице курса («SafetyHub.kz») — провайдер курса стал ссылкой `{ @id }` на организацию: имя физически не может разойтись между главной и страницей курса
- [x] `B3-28` P2 — Course не содержит offers и image, поэтому не проходит требования Google для Course info — `image` добавлен (обложка каталога). `offers` **не добавлял**: на странице нет ни одного видимого утверждения о цене, и объявлять её только в разметке — заявление, которого сайт читателю не делает. Нужно коммерческое решение владельца
- [x] `B3-29` P2 — На главной курсы размечены как набор самостоятельных Course без ItemList и без hasCourseInstance — вместо пяти корневых `Course` один `ItemList` с `ListItem`; проверено на проде: 1 ItemList, 5 ListItem, 5 Course, 5 CourseInstance
- [x] `B3-30` P2 — На каждой публичной странице три несвязанных узла организации; @id LocalBusiness меняется вместе с локалью — единый `@id` организации и сайта, `publisher`/`provider`/`author` ссылаются на него; `websiteJsonLd` перестал зависеть от локали (одна сущность не может иметь четыре url и четыре inLanguage)
- [x] `B3-31` P2 — courseMode стоит на самом Course, а место проведения передано через locationCreated вместо hasCourseInstance.location — `courseMode` и `location` переехали в `hasCourseInstance` (`VirtualLocation`), `locationCreated` с названием города удалён
- [x] `B3-43` P2 — og:image объявлен как 1200×630, но все реальные обложки статей другого размера — 1200×630 объявляется только для сгенерированной карточки; для обложек статей (1200×800) размеры не объявляются вовсе — краулер измеряет сам. Таблицу размеров из рецепта **не заводил**: она разъедется при первой перегенерации обложки молча
- [x] `B3-44` P2 — Суффикс « — SafetyHub.kz» добавляется без бюджета длины: title статей до 101 символа — бюджет 60 символов применяется только к `<title>`; Open Graph и Twitter продолжают получать брендированную форму. Длина самих `seo.title` в контенте (до 86 символов в kk) кодом не лечится — в действия владельца
- [x] `B3-46` P2 — og:type=article на страницах курсов, без article:published_time и вопреки Course JSON-LD — `type: website` на страницах курсов
- [x] `B3-47` P2 — Все description публичных страниц короче 140 символов, сниппет недоиспользован на 40–60 % — раскрыты 7 описаний (Contacts, Faq, Blog, Home, privacy, terms). Из текста Contacts убраны часы работы, чтобы не дублировать единственный источник
- [x] `B3-48` P2 — meta keywords: пустой тег на всех не-RU страницах и один и тот же спам-набор на всех RU — пустой тег `keywords` больше не выводится вовсе (было: пустая строка на всех не-RU страницах). Одинаковый набор `BASE_KEYWORDS` на всех RU-страницах оставил: `meta keywords` не учитывается поисковиками, а раздача по страницам — это правка десятка вызовов ради нулевого эффекта
- [x] `B3-49` P2 — Title страницы контактов дублирует бренд дважды во всех четырёх локалях — «Контакты: телефон и WhatsApp, Алматы» — бренд остался только в суффиксе
- [x] `B3-50` P2 — Административная панель вообще не отдаёт <title> — `title.default` + `template` в `app/(admin)/layout.tsx`
- [x] `B3-51` P2 — Страница проверки сертификата не отдаёт ни Open Graph, ни Twitter Card — Open Graph и Twitter Card на `/verify/[token]`, строго обезличенные: общая карточка, ничего из самого сертификата
- [x] `B3-52` P2 — Общая OG-картинка не содержит ни оффера, ни названия страницы и дважды повторяет бренд — повтор бренда убран. `BRAND.tagline` из рецепта **не подставлял**: он по-русски, а карточка одна на четыре локали (и тест запрещает кириллицу в этом файле). Вместо него локаленезависимый блок «Almaty · Kazakhstan». Полноценное решение — карточка на локаль — в действиях владельца

## Пункт 15. Семантика заголовков и доступность

- [ ] `B3-04` P1 — /profile: инверсия уровней (h3 перед h2) и заголовки секций, свёрстанные как <span>
- [ ] `B3-06` P1 — /auth/legal: на странице ноль h1 — единственный заголовок контента h2
- [ ] `B3-18` P2 — CardTitle жёстко зашит на h3: пропуск уровня h1 → h3 и счётчик, обёрнутый в заголовок
- [ ] `B3-19` P2 — Предпросмотр локализации статьи: h3-заголовок статьи, за которым идут h2 её же разделов
- [ ] `B3-20` P2 — Уровни заголовков в блоках статьи не валидируются: редактор позволяет опубликовать H4 без H2 и скачок H2→H4
- [ ] `B3-21` P2 — /topics/[slug]: единственный заголовок на странице — h1, заголовок CTA-карточки свёрстан <p>, заголовок источников живёт в <summary>
- [ ] `B3-32` P2 — Переходы в кабинет и админку в меню пользователя сделаны через router.push, а не ссылками
- [ ] `B3-34` P2 — Список курсов в кабинете — псевдотаблица из div-заголовков и <article>-строк без связи со столбцами
- [ ] `B4-21` P2 — Аватар: весь встроенный режим камеры (getUserMedia) недостижим — ~150 строк мёртвого кода и лишнее разрешение camera=(self)
- [ ] `B5-06` P1 — Токен --color-success не переопределён в тёмной теме — 2.29:1 в бейдже «Текущая версия»
- [ ] `B5-07` P1 — Модалка камеры в загрузчике аватара: role=dialog/aria-modal без ловушки фокуса, Escape и возврата фокуса
- [ ] `B5-08` P1 — Очередь заявок: поиск и сортировка без доступного имени, у select дополнительно снят индикатор фокуса
- [ ] `B5-09` P1 — Скрытые file-инпуты админки остаются в порядке табуляции без доступного имени
- [ ] `B5-14` P1 — Интерактивные элементы меньше 44×44 CSS-px — полный перечень
- [ ] `B5-22` P1 — На мобильной ширине вся навигация отсутствует в серверном HTML, а входа в аккаунт нет вообще
- [ ] `B5-23` P2 — Textarea причины отказа обёрнута в label, внутри которого лежат кнопки быстрых причин
- [ ] `B5-24` P2 — aria-controls указывает на несуществующие id в трёх раскрывающихся компонентах

## Пункт 16. Производительность: изображения, бандлы, service worker

- [ ] `B2-02` P1 — sharp трассируется только для /api/profile/avatar — два других sharp-роута останутся без libvips на Vercel
- [ ] `B2-14` P2 — readdirSync по content/snapshots/courses затягивает 6,9 МБ PDF курсов в 8 serverless-функций
- [ ] `B2-17` P2 — Манифест локализаций на 1,41 МБ целиком запекается в 8 функций legal-страниц
- [ ] `B7-01` P0 — Hero: <source> в <picture> без sizes — LCP-картинка скачивается дважды на десктопе
- [ ] `B7-08` P1 — LCP-обложка каталога /topics и первая карточка /blog помечены loading="lazy"
- [ ] `B7-11` P1 — Атрибуты sizes задают vw без учёта max-width контейнера 1280px — перекачка обложек на широких экранах
- [ ] `B7-12` P1 — Мобильный hero — портрет 960×1200, а показывается в боксе 16:9: обрезается около 80% скачанных пикселей
- [ ] `B7-15` P1 — Hero: <source> без sizes — десктопный LCP качается дважды и в 1920w вместо 1080w
- [ ] `B7-17` P1 — libphonenumber-js статически в клиентском чанке: 39 КиБ gzip на /profile, /onboarding и /admin/account, трижды продублировано
- [ ] `B7-18` P1 — В HTML каждой страницы вшит весь словарь next-intl (50 КБ), включая серверные неймспейсы; в админке он не нужен вовсе
- [ ] `B7-19` P1 — Service worker задерживает КАЖДЫЙ статический ответ на полную запись в Cache Storage и обход всего кеша
- [ ] `B7-20` P1 — Мёртвая стратегия кеширования изображений в SW: браузер запрашивает /_next/image, а префикс в списке — /images/
- [ ] `B7-21` P1 — Бюджеты бандла не покрывают самые тяжёлые маршруты: редактор курса 404.8 КиБ initial при потолке 200-310 КиБ у покрытых
- [x] `B7-24` P2 — Кнопка скачивания сертификата: таймер на 4 с не очищается, а Worker не прерывается при размонтировании
- [ ] `B7-27` P2 — Кадрирование аватара: перерисовка canvas на каждое движение пальца без склейки по кадрам
- [x] `B7-28` P2 — Фон process-journey.webp (44 КБ, 1400×768) грузится сырым ради слоя с прозрачностью 0.08
- [ ] `B7-29` P2 — Аватары в очереди заявок: <img> без loading="lazy", width/height и decoding — N одновременных админ-вызовов
- [ ] `B7-31` P2 — images.qualities разрешает 8 значений, из которых реально используются 3 — лишние варианты кэша оптимизатора
- [ ] `B7-32` P2 — 245 стран телефонного селектора сериализуются в HTML приватных страниц: ~37 КБ на страницу, дважды
- [ ] `B7-33` P2 — Общий CSS-бандл занял 89.5% бюджета: 2.1 КиБ gzip запаса на весь проект
- [ ] `B7-34` P2 — @radix-ui/react-label ради обычного <label>: три серверные админ-страницы становятся клиентскими без причины

## Пункт 17. Мёртвый код и зависимости

- [ ] `B1-06` P1 — Мёртвые правовые компоненты v1.4/v2.4, которые вдобавок собирают документ без runtime-валидации ссылок
- [ ] `B1-40` P2 — Мёртвая подсистема WebAuthn: 4 модуля и 2 продакшн-зависимости остались после вывода паскейов из эксплуатации
- [ ] `B1-51` P2 — lib/telegram.ts — мёртвый модуль с ложным JSDoc, дублирующий telegram-dispatcher
- [ ] `B2-06` P2 — Мёртвые компоненты юридических документов privacy-policy-v1-4.tsx и terms-policy-v2-4.tsx
- [ ] `B2-07` P2 — Мёртвый компонент components/admin/admin-action-dialog.tsx (178 строк) — тест доступности проверяет несуществующий в бандле UI
- [ ] `B2-09` P2 — Проп compact у AppErrorState не работает: конфликт max-w без twMerge; проп error фактически не используется
- [ ] `B2-10` P2 — Неиспользуемые экспорты UI-кита: buttonVariants, шесть примитивов dropdown-menu и CardFooter
- [ ] `B2-11` P2 — Недостижимые варианты дизайн-системы: Button variant uppercase и link, Badge variant primary
- [ ] `B2-12` P2 — Пропы, которые принимаются, но никем не передаются: PageHeader.actions, EmptyState.icon/action, SectionShell.innerClassName
- [ ] `B2-18` P2 — @simplewebauthn/browser — неиспользуемая production-зависимость (ноль импортов во всём репозитории)
- [ ] `B2-19` P2 — Мёртвый остров WebAuthn: 1230 строк features/auth/zh-webauthn-* без единого потребителя
- [ ] `B2-20` P2 — Три привилегированные админ-мутации в features/admin/attestations.ts мертвы и обходят идемпотентный конверт
- [ ] `B2-21` P2 — lib/telegram.ts — мёртвый модуль, читающий TELEGRAM_BOT_TOKEN, при живом Edge Function-диспетчере
- [ ] `B2-22` P2 — i18n/routing.ts объявляет конфигурацию next-intl, которая никогда не применяется
- [ ] `B2-23` P2 — lib/validation/auth.ts: 14 из 16 экспортов — мёртвые парольные схемы после отключения парольной аутентификации
- [ ] `B2-24` P2 — features/admin/data.ts: четыре читающих экрана админки удалены, а их серверные функции и парсеры остались
- [ ] `B2-26` P2 — lib/utils.ts: generateCertificateNumber на Math.random и seededShuffle — мёртвые генераторы предметной логики
- [ ] `B2-27` P2 — lib/navigation.js — единственный .js-модуль в TypeScript-дереве, импортируемый клиентским компонентом без типов
- [ ] `B2-28` P2 — Два теста лежат внутри features/, нарушая соглашение о расположении тестов в tests/
- [ ] `B2-29` P2 — Четыре мёртвые экспортированные функции в lib/ и features/, не вызываемые даже внутри своих файлов
- [ ] `B2-30` P2 — lib/constants.ts: @deprecated-алиас QUIZ без потребителей и ROUTES.resetPassword, ведущий на 410-надгробие
- [ ] `B2-40` P2 — lib/navigation.js — нетипизированный JS в строгом TS-проекте, и та же логика продублирована в админке
- [ ] `B2-42` P2 — Журнал администратора доступен по двум URL; ссылки пагинации со /admin/audit уводят на /admin/settings/history
- [ ] `B2-43` P2 — Пять копий *Href + курсорно-хлебного каркаса в админских списках; в одной из них курсор кодируется иначе
- [ ] `B2-45` P2 — privacy/[version] и terms/[version] — четыре побайтово одинаковых файла, различающихся одним строковым литералом
- [ ] `B2-47` P2 — Шесть маршрутов дублируют предобработчик «message.includes(CODE) → 409» вместо реестра конфликтов в apiError
- [ ] `B2-48` P2 — «Стеклянная» карточка продублирована 7 раз мимо токенов --radius-card/--shadow-card; тень не адаптируется к тёмной теме
- [ ] `B2-49` P2 — Компонент SectionHeading существует, но два маркетинговых блока переписали заголовок вручную с разъехавшимися размерами
- [ ] `B2-51` P2 — Оболочка юридического документа продублирована в четырёх «замороженных» рендерерах и в двух модулях страниц; страница импортируется компонентом

## Пункт 18. Порядок проверок в route handlers

- [ ] `B2-03` P1 — Каркас route handler скопирован 41 раз; порядок origin→parse→authz→quota разный, тело парсится до проверки прав
- [ ] `B2-41` P2 — zod и словарь серверных кодов ошибок в клиентском компоненте очереди заявок
- [ ] `B2-44` P2 — Список локалей продублирован 20+ раз вместо APP_LOCALES; второй тип AppLocale в lib/supabase/types.ts, а два теста закрепляют дубликат

## Пропущено осознанно

- [-] `B1-01` P0 — Боевые production-секреты Supabase (service_role, пароль БД, Turnstile) лежат в .env.local и автоматически подхватываются dev-сборкой и npm-скриптами
  - Причина: Требует ротации секретов владельцем — вне репозитория (см. пункт 19).
- [-] `B1-03` P0 — В БД actor_has_capability сведена к product_role='admin': admin_default и user_capabilities игнорируются, любой админ получает role.manage, user.delete, capability.manage и audit.read
  - Причина: Решение владельца: модель «все админы равны» сохраняется, матрица не восстанавливается.
- [-] `B1-13` P1 — Черновик онбординга с ПДн в localStorage без привязки к пользователю: предзаполняет форму следующего аккаунта
  - Причина: Совпадает с B4-37 — чинится там же (пункт 9).
- [-] `B2-15` P2 — 106 МБ бинарного content/ в обычном git без LFS: 55 PPTX/PDF, рост +57 МБ на каждую партию локализации
  - Причина: Git LFS: нужен платный пакет и переписывание истории с force-push.
- [-] `B2-16` P2 — 22,78 МБ побайтовых дубликатов PDF: staged-партия и опубликованный снапшот хранят одни и те же 15 файлов
  - Причина: Дедупликация staged/snapshot возможна только вместе с B2-15.
- [-] `B2-31` P2 — components/ импортирует модуль маршрута app/: инверсия слоёв в static-legal-document
  - Причина: Чистая инверсия слоёв без пользовательского эффекта.
- [-] `B2-32` P2 — Клиентские компоненты импортируют модули с 'server-only' — сборка держится только на стирании типов
  - Причина: Чистая перестройка границ слоёв.
- [-] `B2-33` P2 — Страницы админки обращаются к БД сервисным ключом напрямую, минуя слой features/admin
  - Причина: Чистая перестройка слоёв (страницы админки → features/admin).
- [-] `B2-34` P2 — lib/ зависит от features/ — нижний слой импортирует верхний (3 модуля)
  - Причина: Чистая инверсия слоёв lib → features.
- [-] `B2-35` P2 — Граница features/ и components/ не определена: 15 UI-компонентов живут в features/ и тянут components/ui
  - Причина: Перенос 15 UI-компонентов между каталогами.
- [-] `B2-36` P2 — components/admin: 41 файл плоским списком, 11 184 строки, без группировки по подсистемам
  - Причина: Перекладывание 41 файла components/admin по подпапкам.
- [-] `B2-37` P2 — features/admin/server.ts — god-модуль: 890 строк, 20 экспортов из шести разных поддоменов
  - Причина: Дробление features/admin/server.ts (890 строк).
- [-] `B2-38` P2 — Границы слоёв не проверяются машинно: eslint.config.mjs не содержит ни одного правила импортов
  - Причина: Lint-правила слоёв не включаются без B2-31/B2-34.
- [-] `B2-39` P2 — Два стиля импортов сосуществуют: алиас @/ и относительные пути с расширением .ts (28 мест)
  - Причина: Унификация стиля импортов в 28 местах.
- [-] `B4-26` P2 — Шесть компонентов-монолитов: метрики и план декомпозиции по файлам и сигнатурам хуков
  - Причина: Декомпозиция шести компонентов-монолитов.
- [-] `B4-30` P2 — AttestationsManager: 26 хуков и семь обязанностей в одном файле — план декомпозиции
  - Причина: Декомпозиция AttestationsManager (1071 строка).
- [-] `B4-31` P2 — QuizClient: 37 хуков, шесть точек возврата и дублирование каждого поля состояния в ref — план декомпозиции
  - Причина: Декомпозиция QuizClient (997 строк).
- [-] `B4-32` P2 — TestEditor: 17 хуков, 500 строк JSX и мутации банка вопросов в теле компонента — план декомпозиции
  - Причина: Декомпозиция TestEditor (980 строк).
- [-] `B4-37` P2 — Черновик онбординга с персональными данными пишется в общий ключ localStorage и не очищается при уходе
  - Причина: Чинится в пункте 9 вместе с B1-13.
